<div class="wrapper wrapper-content">

	<%= render 'widget' %>
	<%= render 'total_amount_by_companies' %>

	<div class="row">
		<%= render 'amount_by_year_last_12' %>
	</div>
	<div class="row">
		<%= render 'amount_by_company_last_12' %>
	</div>

	
</div>
